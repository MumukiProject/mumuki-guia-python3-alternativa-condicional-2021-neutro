---
rules:
- when:
    these_expectations_failed:
    - escribir_cartelito_optimo Not:UsesIf
  then: "`escribir_cartelito` ya tiene un `if`, ahora solamente tenés que resolver
    qué condición le pasas como argumento"
